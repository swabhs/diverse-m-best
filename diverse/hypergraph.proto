package diverse;

message edge {
  required string id = 1;
  required double weight = 2;
}

message node {
  required string id = 1;
  optional edge parent = 2;
  repeated edge child = 3;
} 

message hypergraph {
  repeated node nodes = 1;
  repeated edge edges = 2;
}
